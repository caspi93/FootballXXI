
<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Football XXI</title>
    @Styles.Render("~/Content/css")
    <!--@Scripts.Render("~/bundles/modernizr")-->

</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light fixed-top py-3" id="mainNav">
        <div class="container">
            <a class="navbar-brand js-scroll-trigger" href="/">Football XXI</a>
            <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ml-auto my-2 my-lg-0">

                    @if (Session["empleado"] != null) {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Administrar
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                @{
                                    var atributosDropdown = new Dictionary<string, object> {
                                    {"class", "dropdown-item" }
                                };
                                }

                                @Html.ActionLink("Crear Empleado", "CrearEmpleado", "Administrar", null, atributosDropdown)
                                @Html.ActionLink("Ver Empleados", "VerEmpleados", "Administrar", null, atributosDropdown)
                                @Html.ActionLink("Ver Reportes", "DatosReportes", "Administrar", null, atributosDropdown)
                                @Html.ActionLink("Pago de Nómina", "Pagos", "Administrar", null, atributosDropdown)
                            </div>
                        </li>
                    }
                    @{
                        var atributosNavLink = new Dictionary<string, object> {
                                    {"class", "nav-link" }
                        };
                    }

                    <li class="nav-item">
                        <a class="nav-link js-scroll-trigger" href="#contac">Contactos</a>
                    </li>
                    @if (Session["tipo"] == null) {
                        <li class="nav-item">
                            @Html.ActionLink("Registrarse", "CrearCliente", "Home", null, atributosNavLink)
                        </li>
                    }
                    @if (Session["tipo"] == null) {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Iniciar Sesión
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                @{
                                    var atributosDropdown = new Dictionary<string, object> {
                                    {"class", "dropdown-item" }
                                };
                                }

                                @Html.ActionLink("Como Administrador", "IniciarSesion", "Login", null, atributosDropdown)
                                @Html.ActionLink("Como Cliente", "IniciarSesionCliente", "Login", null, atributosDropdown)
                            </div>
                        </li>
                    }
                    @if (Session["tipo"] != null) {
                        <li class="nav-item">
                            @Html.ActionLink("Cerrar Sesión", "CerrarSesion", "Login", null, atributosNavLink)
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <!-- Masthead -->
    <header class="masthead">

        @RenderBody()

    </header>
    <div class="container body-content">
        <hr />
        <!-- Footer -->
        <footer class="bg-light py-5">
            <div class="container">
                <div class="small text-center text-muted">Copyright &copy; 2019 - Start Bootstrap</div>
            </div>
        </footer>
    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jquery-easing")
    @Scripts.Render("~/bundles/jquery-magnific")
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/creative")
    @RenderSection("scripts", required: false)

</body>
</html>
